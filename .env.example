# ===============================================================================
# 🚀 COWORKING MANAGEMENT SYSTEM - КОНФИГУРАЦИЯ
# ===============================================================================

# 📋 ОСНОВНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ
# ⚠️  Для продакшена скрипт автоматически изменит DEBUG, ENVIRONMENT, BUILD_TARGET
DEBUG=false
ENVIRONMENT=development
BUILD_TARGET=development
HOST=0.0.0.0
PORT=8000
APP_NAME="Coworking API"
APP_VERSION="1.0.0"

# 🌐 ДОМЕН (ОБЯЗАТЕЛЬНО ДЛЯ ПРОДАКШЕНА!)
# Укажите ваш домен или IP адрес сервера
# Примеры: example.com, coworking.ru, 192.168.1.100
DOMAIN_NAME=your-domain.com

# 🔗 СЕТЕВЫЕ НАСТРОЙКИ
# ⚠️  Эти параметры автоматически настраиваются скриптами!
# Не изменяйте их вручную, если не знаете что делаете
API_BASE_URL=http://localhost:8000
API_BASE_URL_EXTERNAL=http://localhost/api
FRONTEND_URL=http://localhost
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173

# 🔐 БЕЗОПАСНОСТЬ
# ⚠️  ОБЯЗАТЕЛЬНО измените для продакшена!
# Команда для генерации: python -c "import os; print(os.urandom(32).hex())"
# Скрипт setup-production.sh автоматически сгенерирует безопасные ключи
SECRET_KEY=your-secret-key-change-me
SECRET_KEY_JWT=your-jwt-secret-key-change-me

# Срок действия токенов
ACCESS_TOKEN_EXPIRE_HOURS=24
REFRESH_TOKEN_EXPIRE_DAYS=7

# 🧪 ТЕСТОВЫЙ РЕЖИМ для проверки refresh токена
# Раскомментируйте следующую строку для быстрой проверки обновления токена
# Access token будет истекать через N минут вместо 24 часов
# ACCESS_TOKEN_EXPIRE_MINUTES=2

# 🛡️ CAPTCHA (hCaptcha) - защита от brute force атак на login
# Получите ключи на https://www.hcaptcha.com/ (бесплатный tier доступен)
# Если не указаны - CAPTCHA отключен (не рекомендуется для продакшена)
HCAPTCHA_SECRET_KEY=your-hcaptcha-secret-key
HCAPTCHA_SITE_KEY=your-hcaptcha-site-key
CAPTCHA_FAILED_ATTEMPTS_THRESHOLD=3

# 👤 АДМИНИСТРАТОР
# ⚠️  ОБЯЗАТЕЛЬНО измените для продакшена!
ADMIN_LOGIN=admin
ADMIN_PASSWORD=admin

# 🤖 TELEGRAM BOT (ОБЯЗАТЕЛЬНО!)
# 1. Создайте бота в @BotFather и получите токен
# 2. Узнайте ваш Telegram ID у @userinfobot
# 3. Создайте группу для уведомлений и добавьте туда бота как админа
BOT_TOKEN=your-bot-token-here
ADMIN_TELEGRAM_ID=your-telegram-id
BOT_LINK=https://t.me/your_bot
INVITE_LINK=https://t.me/your_bot
GROUP_ID=your-group-id

# 📝 TELEGRAM ЛОГИРОВАНИЕ (опционально)
# Создайте отдельный канал для логов ошибок
TELEGRAM_LOGGING_ENABLED=true
FOR_LOGS=your-logs-channel-id
TELEGRAM_LOG_MIN_LEVEL=ERROR
TELEGRAM_LOG_RATE_LIMIT=5

# 💳 YOOKASSA (ПЛАТЕЖИ)
# Получите данные в личном кабинете YooKassa
# https://yookassa.ru/developers/
YOKASSA_ACCOUNT_ID=your-account-id
YOKASSA_SECRET_KEY=your-secret-key

# 🏢 RUBITIME (ВНЕШНЯЯ СИСТЕМА БРОНИРОВАНИЯ)
# Получите API ключ и ID в личном кабинете Rubitime
RUBITIME_API_KEY=your-api-key
RUBITIME_BASE_URL=https://rubitime.ru/api2/
RUBITIME_BRANCH_ID=12595
RUBITIME_COOPERATOR_ID=25786

# 🗄️ БАЗА ДАННЫХ
# Настройки SQLite (не изменяйте без необходимости)
DB_TIMEOUT=60
DB_RETRY_ATTEMPTS=3
DB_RETRY_DELAY=0.1

# Connection Pool Configuration (P-MED-2: оптимизировано для production)
# Рекомендуемые значения для production: POOL_SIZE=20, MAX_OVERFLOW=30
# Development: можно оставить по умолчанию (10+15)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=30
DB_POOL_TIMEOUT=45
DB_POOL_RECYCLE=1800

# 📁 ФАЙЛЫ И ЛИМИТЫ
FILE_RETENTION_DAYS=30
MAX_FILE_SIZE_MB=10
NEWSLETTER_MAX_PHOTOS=10
NEWSLETTER_MAX_FILE_SIZE_MB=20
NEWSLETTER_RATE_LIMIT_DELAY=0.05

# 📋 ЛОГИРОВАНИЕ
# Для продакшена рекомендуется LOG_FORMAT=json
LOG_LEVEL=INFO
LOG_FORMAT=text
LOG_TO_FILE=true

# 🎯 ФИЛЬТРАЦИЯ ЛОГОВ И МЕТРИКИ ПРОИЗВОДИТЕЛЬНОСТИ
# Исключить часто вызываемые endpoints из логирования (для уменьшения verbose логов)
# Формат: пути через запятую, например: /notifications/check_new,/health,/metrics
EXCLUDE_PATHS_FROM_LOGGING=/notifications/check_new,/health

# Уровень логирования для middleware (INFO или WARNING)
# WARNING - логировать только ошибки и медленные запросы
# INFO - логировать все запросы (verbose, для development)
MIDDLEWARE_LOG_LEVEL=INFO

# Порог для определения медленных запросов (в миллисекундах)
# Запросы медленнее этого значения будут логироваться как WARNING
LOG_SLOW_REQUEST_THRESHOLD_MS=1000

# 🔴 REDIS КЭШИРОВАНИЕ
# Не изменяйте REDIS_URL - он автоматически настраивается
REDIS_URL=redis://redis:6379/0

# Redis Eviction Policy (P-MED-3: оптимизировано)
# volatile-lru - удаляет ключи с истекшим TTL по принципу LRU (least recently used)
# Рекомендуется для приложений где все ключи имеют TTL (как в нашем случае)
# Альтернативы: allkeys-lru (для ключей без TTL), volatile-ttl, noeviction
REDIS_MAXMEMORY_POLICY=volatile-lru

CACHE_DEFAULT_TTL=300
CACHE_DASHBOARD_TTL=60
CACHE_USER_DATA_TTL=600
CACHE_STATIC_DATA_TTL=1800

# 🔗 ВНЕШНИЕ ССЫЛКИ
# Укажите ссылки на ваши ресурсы
ADMIN_URL=https://t.me/your_admin
RULES_URL=https://example.com/rules

# 📧 EMAIL / SMTP НАСТРОЙКИ (для email рассылок)
# Яндекс SMTP (рекомендуется создать пароль приложения)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USE_SSL=true
SMTP_USE_TLS=false
SMTP_USERNAME=noreply@yourdomain.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=Coworking Space
SMTP_TIMEOUT=30
SMTP_MAX_RETRIES=3

# Email лимиты
EMAIL_BATCH_SIZE=50
EMAIL_BATCH_DELAY=1
EMAIL_RATE_LIMIT_PER_MINUTE=100

# 📊 EMAIL TRACKING - Домен для трекинга открытий и кликов
# ⚠️  ВАЖНО: Должен быть публично доступен из интернета!
# Для localhost разработки используйте ngrok: EMAIL_TRACKING_DOMAIN=https://your-id.ngrok.io
# Для production используйте ваш домен: EMAIL_TRACKING_DOMAIN=https://yourdomain.com
# Если оставить пустым, будет использован FRONTEND_URL (рекомендуется)
EMAIL_TRACKING_DOMAIN=

# ===============================================================================
# 📋 ИНСТРУКЦИИ ПО НАСТРОЙКЕ:
# 1. Обязательно заполните: DOMAIN_NAME, BOT_TOKEN, ADMIN_TELEGRAM_ID, GROUP_ID
# 2. Измените ADMIN_LOGIN и ADMIN_PASSWORD
# 3. Заполните платежные системы если нужны
# 4. Запустите: ./scripts/setup-production.sh (сгенерирует безопасные ключи)
# 5. Запустите: ./start.sh
# ===============================================================================